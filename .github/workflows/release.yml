name: Release QGIS Plugin

on:
  push:
    tags:
      - '*'

jobs:
  build-release:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install zip
        run: sudo apt-get install zip -y

      - name: Build plugin zip
        run: |
          mkdir -p IGN_Vues
          rsync -av --progress . IGN_Vues/ \
            --exclude .git \
            --exclude .github \
            --exclude IGN_Vues
          zip -r IGN_Vues.zip IGN_Vues

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: "IGN_Vues.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}